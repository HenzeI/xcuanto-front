# URL base de la API Django (para futura integración)
VITE_API_URL=http://localhost:8000/api

# Modo de datos: 'mock' usa datos locales | 'api' conecta con Django
VITE_DATA_MODE=mock

# URL del logo (opcional - si no se define, se muestra el texto "xcuanto")
# VITE_LOGO_URL=/logo.png
